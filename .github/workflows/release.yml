name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  publish-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Build, Test, and Publish
        # The tag is the version. Passing it here is what makes the binary report the same
        # number in its MCP handshake as the release it came from.
        run: dotnet run --project build -- --target=Publish --release-version=${{ github.ref_name }}

      - name: Rename binaries with platform suffixes
        run: |
          cp publish/win-x64/pondhawk-generation-mcp.exe publish/pondhawk-generation-mcp-win.exe
          cp publish/osx-arm64/pondhawk-generation-mcp publish/pondhawk-generation-mcp-osx
          cp publish/linux-x64/pondhawk-generation-mcp publish/pondhawk-generation-mcp-linux
          cp publish/linux-arm64/pondhawk-generation-mcp publish/pondhawk-generation-mcp-linux-arm

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >
          gh release create "${{ github.ref_name }}"
          publish/pondhawk-generation-mcp-win.exe
          publish/pondhawk-generation-mcp-osx
          publish/pondhawk-generation-mcp-linux
          publish/pondhawk-generation-mcp-linux-arm
          --title "${{ github.ref_name }}"
          --generate-notes
